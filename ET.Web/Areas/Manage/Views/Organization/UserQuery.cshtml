@{
    Layout = null;
    Layout = "~/Areas/Manage/Views/Shared/_ManagePartial.cshtml";
}
@section Title{
    博客文章列表
}
@section Header{
    <link href="@Url.Content("~/Res/autocomplete/autocomplete.css")" rel="stylesheet" type="text/css" />
    <script src="@Url.Content("~/Res/autocomplete/jquery.autocomplete.js")" type="text/javascript"></script>
}
<div class="ManageBody">
    <div class="ManageListTitle"></div>
    <fieldset class="ManageSearch" style=" ">
        <legend>信息查询</legend>
        <table class="datatable">
            <tr>
                <th style="width:80px;">
                    用户姓名：
                </th>
                <td style="width:120px;">
                    <input id="itxtSearch" style="width: 175px" type="text" data-options="prompt:'请输入查询内容'"
                           class="search input" />

                </td>
                <th style="width:60px;">
                    性别：
                </th>
                <td style="width:60px;">

                    <select id="iSex"><option value="">全部</option><option value="男">男</option><option value="女">女</option><option value="保密">保密</option></select>
                </td>
                <td>
                    <a href='javascript:queryClick()' class='easyui-linkbutton' data-options='iconCls:"icon-search"'>
                        查询
                    </a>
                </td>
            </tr>
        </table>
    </fieldset>
    <div class="ManageList">
        <table id="InfoGridData"></table>
    </div>
</div>

<script type="text/javascript">
    $(function () {
        var toolbarnode = [
            {
                text: '添加',
                iconCls: 'icon-add',
                handler: fixAdd
            }, '-', {
                text: '查看',
                iconCls: 'icon-view',
                handler: fixView
            }, '-', {
                text: '修改',
                iconCls: 'icon-edit',
                handler: fixEdit
            }, '-', {

                text: '禁用',
                iconCls: 'icon-no',
                handler: fixDisable
            }, '-', {
                text: '启用',
                iconCls: 'icon-ok',
                handler: fixEnabled
            }, '-', {
                text: '删除',
                iconCls: 'icon-remove',
                handler: fixDel
            }, '-'
        ]
        $('#InfoGridData').datagrid({
            width: $(document).width() - 10,
            height: 380,
            fitColumns: false,
            striped: true,
            nowrap: false,
            showFooter: true,
            url: "/Organization/AjaxQueryUserPageList", method: 'get',
            collapsible: false,
            remoteSort: false,
            showFooter: true,
            singleSelect: false,
            pagination: true,
            rownumbers: true,
            pageSize: 10,
            columns: [[
                    { field: 'UserID', width: 10, title: 'ID', hidden: true },
                    { field: 'CNName', width: 100, title: '姓名', sortable: true },
                     { field: 'Sex', width: 50, title: '性别', sortable: true },
                      { field: 'Age', width: 50, title: '年龄', sortable: true },
                      { field: 'Mobile', width: 100, title: '手机', sortable: true },
                      { field: 'LiveArea', width: 120, title: '居住地', sortable: true },
                       { field: 'Source', width: 100, title: '来源', sortable: true },
                    {
                        field: 'CreateTime', width: 120, title: '创建时间', sortable: true,
                        formatter: DateHelper.DateTimeFormatter
                    }, { field: 'Status', width: 50, title: '状态', sortable: true, formatter: formatterDoState }
            ]],
            toolbar: toolbarnode

        });
        var options = { serviceUrl: '/Organization/AjaxSearchUser' };
        searchobj = $('#itxtSearch').autocomplete(options);
    });
    function formatterDoState(val, row, index) {
        if (val == "1")
            return '<img src="/images/public/icon-ok.png" />'
        else if (val == "0")
            return '<img src="/images/public/icon-filter.png" />'
        else
            return '<img src="/images/public/icon-no.png" />'

    }
    function fixAdd() {
        openWindow('新增', "UserManage", { width: 800, height: 450 })
    };

    function fixDel() {
        var selected = $('#InfoGridData').datagrid('getChecked');
        if (null == selected || selected.length < 1) {
            TopNotify("请选择数据", 'info');
            return;
        }
        $.messager.confirm("确认提示框", "确认删除这 " + selected.length + " 条吗？", function (r) {
            if (r) {
                var ls = $.map(selected, function (a) { return "'" + a.UserID + "'"; }).toString();
                $.post('/Organization/AjaxDeleteUser', { ids: ls }, function (res) {
                    if (res == "true") {
                        $("#InfoGridData").datagrid('reload')
                        TopNotify("数据已删除", 'info');
                    }
                    else {
                        TopNotify(res, 'error');
                    }
                });
            }
        });
    };
    function fixDisable() {
        var selected = $('#InfoGridData').datagrid('getChecked');
        if (null == selected || selected.length < 1) {
            TopNotify("请选择数据", 'info');
            return;
        }
        $.messager.confirm("确认提示框", "确认禁用这 " + selected.length + " 条吗？", function (r) {
            if (r) {
                var ls = $.map(selected, function (a) { return "'" + a.UserID + "'"; }).toString();
                $.post('/Organization/AjaxDisableUser', { ids: ls }, function (res) {
                    if (res == "true") {
                        $("#InfoGridData").datagrid('reload')
                        TopNotify("状态已改变", 'info');
                    }
                    else {
                        TopNotify(res, 'error');
                    }
                });
            }
        });
    };
    function fixEnabled() {
        var selected = $('#InfoGridData').datagrid('getChecked');
        if (null == selected || selected.length < 1) {
            TopNotify("请选择数据", 'info');
            return;
        }
        $.messager.confirm("确认提示框", "确认启用这 " + selected.length + " 条吗？", function (r) {
            if (r) {
                var ls = $.map(selected, function (a) { return "'" + a.UserID + "'"; }).toString();
                $.post('/Organization/AjaxEnabledUser', { ids: ls }, function (res) {
                    if (res == "true") {
                        $("#InfoGridData").datagrid('reload')
                        TopNotify("状态已改变", 'info');
                    }
                    else {
                        TopNotify(res, 'error');
                    }
                });
            }
        });
    };
    function fixEdit() {
        var selected = $('#InfoGridData').datagrid('getSelected');
        if (null == selected || selected.length < 1) {
            TopNotify("请选择数据", 'info');
            return;
        }
        openWindow('修改', "UserManage?infoid=" + selected.UserID, { width: 800, height: 450 })
    };

    function fixView() {
        var selected = $('#InfoGridData').datagrid('getSelected');
        if (null == selected || selected.length < 1) {
            TopNotify("请选择数据", 'info');
            return;
        }
        openSimpleWindow('查看', "UserManage?infoid=" + selected.UserID, { width: 800, height: 450 })
    };

    function queryClick() {
        $('#InfoGridData').datagrid('load', getQueryParam());
    };

    function getQueryParam() {
        var query = { name: $('#itxtSearch').val(), sex: $('#iSex').val() };
        return query;
    };
</script>